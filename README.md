# Wasm Template
Templates for bootstrapping egui and Cloudflare Workers projects.

## Quickstart

```sh
cargo install trunk
cd template-app
trunk serve
```

```sh
npm install -g wrangler
cd template-worker
wrangler dev --local
```
